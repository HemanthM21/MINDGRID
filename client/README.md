# LifeOS Frontend

This is a minimal Vite + React scaffold for the LifeOS frontend used to display Tasks and Dashboard.

Install dependencies and run dev server:

```bash
cd client
npm install
npm run dev
```

The dev server proxies `/api` to `http://localhost:5000`.
